package com.deal_stage_flow.demo.enums;

public enum SalesPipelineStage {
    //currently I used same as hubspot->we will change according to unific
    APPOINTMENT_SCHEDULED,
    QUALIFIED_TO_BUY,
    PRESENTATION_SCHEDULED,
    DECISION_MAKER_BOUGHT_IN,
    CONTRACT_SENT,
    CLOSED_WON,
    CLOSED_LOST;
}
